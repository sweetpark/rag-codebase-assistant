<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lab.demo_ai.AccountRepository">

    <!--
    [기능요약] 계정 목록 조회
    [상세설명] member 테이블에서 전체 계정 정보를 조회한다
    -->
    <select id="sltAccountInfo" parameterType="map" resultType="map">
        SELECT id, username, company_name, ceo_name, email, phone, biz_no
        FROM member
    </select>

    <!--
    [기능요약] 계정 목록 상세 조회
    [상세설명] member 테이블에서 계정 정보 한 개를 조회한다
    -->
    <select id="sltAccountOne" parameterType="map" resultType="map">
        SELECT id, username, company_name, ceo_name, email, phone, biz_no
        FROM member
        WHERE id = #{id}
    </select>

    <!--
    [기능요약] 계정 등록
    [상세설명] member 테이블에 계정 정보를 등록한다
    -->
    <insert id="istAccount" parameterType="map">
        INSERT INTO member (id, username, company_name, ceo_name, email, phone, biz_no)
               VALUES (#{id}, #{username}, #{companyNm}, #{ceoNm}, #{email}, #{phone}, #{biz_no})
    </insert>

    <!--
    [기능요약] 계정 수정
    [상세설명] member 테이블에 계정 정보를 수정한다
    -->
    <update id="uptAccount" parameterType="map">
        UPDATE member
        SET
            username = #{username},
            company_name = #{companyNm},
            ceo_name = #{ceoNm},
            email = #{email},
            phone = #{phone},
            biz_no = #{biz_no}
        WHERE id = #{id}
    </update>

    <!--
    [기능요약] 계정 패스워드 초기화
    [상세설명] member 테이블에서 pasword 컬럼값을 '1111'로 초기화한다
    -->
    <update id="resetPw" parameterType="map">
        UPDATE member
        SET
            password = '1111'
        WHERE id = #{id}
    </update>

    <!--
    [기능요약] 중복 계정 조회
    [상세설명] member 테이블에서 등록할 계정 정보가 존재하는지 확인한다
    -->
    <select id="dupCheck" parameterType="map" resultType="map">
        SELECT COUNT(*) AS CNT
        FROM member
        WHERE id=#{id}
    </select>
</mapper>